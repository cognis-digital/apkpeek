<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leakybank">

    <uses-sdk android:minSdkVersion="19" android:targetSdkVersion="22" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:label="LeakyBank"
        android:debuggable="true"
        android:allowBackup="true"
        android:usesCleartextTraffic="true">

        <!-- exported with intent-filter, no permission guard -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- exported activity reachable by other apps -->
        <activity android:name=".AdminPanelActivity" android:exported="true" />

        <!-- exported service with no permission -->
        <service android:name=".PaymentService" android:exported="true" />

        <!-- exported receiver listening on SMS broadcast -->
        <receiver android:name=".SmsReceiver">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- exported content provider with no permission (critical) -->
        <provider
            android:name=".AccountProvider"
            android:authorities="com.example.leakybank.accounts"
            android:exported="true" />

        <!-- properly locked-down internal activity -->
        <activity android:name=".InternalSettings" android:exported="false" />

        <!-- hard-coded secrets in meta-data -->
        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="AIzaEXAMPLE0EXAMPLE0EXAMPLE0EXAMPLE0EXA" />
        <meta-data android:name="aws_access"
            android:value="AKIAIOSFODNN7EXAMPLE" />
    </application>
</manifest>
